# Supabase

Org already on this account: **O360 Core Websites**.

Existing projects (do not reuse for o360.com):

- Orbit101 (`jxwrsofxzphqqoluosur`)
- Dental Country (`eelvvqrqqubwizlfupai`)

Create a new project named **O360** in that org, then:

1. Put URL + publishable key in `.env.local`
2. Run `supabase/migrations/20260814120000_site_pages.sql`

Until then the homepage reads from `content/home.ts`.
