# Temporary working files / deliverables (kept local, not committed)
WORKING-NOTES.md
o360-rankmath-redirects-by-hits.csv
project-category-missing-landing-pages.md

# Secrets — never commit
.env
.env.*
!.env.example
